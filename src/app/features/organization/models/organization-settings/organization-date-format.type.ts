/**
 * Type OrganizationDateFormat
 * @type OrganizationDateFormat
 *
 * @description
 * Supported date format patterns for an organization workspace. Values mirror
 * the catalog accepted by the API.
 */
export type OrganizationDateFormat =
  | 'dd/MM/yyyy'
  | 'MM/dd/yyyy'
  | 'yyyy-MM-dd'
  | 'dd.MM.yyyy'
  | 'dd-MM-yyyy';
