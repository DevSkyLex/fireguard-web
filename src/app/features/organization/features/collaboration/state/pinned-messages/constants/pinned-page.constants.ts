/**
 * Constant PINNED_PAGE_SIZE
 * @const PINNED_PAGE_SIZE
 *
 * @description
 * Pinned messages fetched in the info sheet's single read — the server's hard
 * cap, so every realistic channel arrives whole.
 *
 * @since 1.0.0
 *
 * @type {number}
 */
export const PINNED_PAGE_SIZE: number = 100;
