import type { TokenType } from '@features/auth/models';

/**
 * Constant TOKEN_TYPE
 *
 * @description
 * OAuth2 Bearer token type constant.
 *
 * @since 1.0.0
 *
 * @type {TokenType}
 */
export const TOKEN_TYPE: TokenType = 'Bearer';
