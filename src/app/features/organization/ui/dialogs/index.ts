/**
 * Public surface of the organization feature's dialog units
 * (ARCHITECTURE.md §13.2): the concern-level entry point for the dialogs
 * consumed across multiple pages and nested subfeatures.
 *
 * @since 1.2.0
 */
export { OrganizationDeleteDialog } from './organization-delete-dialog';
export { OrganizationQuotaUpgradeDialog } from './organization-quota-upgrade-dialog';
