/**
 * Enum families covered by the account status tag registry.
 *
 * A single family today; the kind is kept so a second account-level enum can
 * join the registry without changing every call site.
 */
export type AccountStatusTagKind = 'accountStatus';
