---
description: Create an Angular directive as a complete unit folder — behavioral (SSR-safe) or template-marker with a typed ngTemplateContextGuard. The repo has zero directives today, so the first one sets the precedent.
argument-hint: '<name> [--context-guard] — e.g. "auto-focus" or "board-card --context-guard"'
---

Delegate to the **fg-directive-builder** subagent: $ARGUMENTS

The agent carries the two anatomies (behavioral vs template-marker), the naming rules, and the SSR guard requirement; do not restate them. The repo has **no directive to mirror** — the agent's own templates are the exemplar.

Require its report to state the **kind** and the placement with the rule that drove it, flag explicitly that a new precedent landed, list the files created (absolute paths), and give the format/lint/test/build results.
