/**
 * Type SlotPresentation
 * @type SlotPresentation
 *
 * @description
 * Visual anatomy requested by a slot outlet for its contributed components.
 * `default` preserves the component's standalone trigger, while `menu` asks
 * action contributions to expose Spartan item rows.
 *
 * @since 1.0.0
 */
export type SlotPresentation = 'default' | 'menu';
