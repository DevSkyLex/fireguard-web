/**
 * Constant SAVED_MESSAGES_PAGE_SIZE
 * @const SAVED_MESSAGES_PAGE_SIZE
 *
 * @description
 * Saved messages fetched per page — the server default, kept explicit so the
 * store's paging arithmetic never depends on a default it did not choose.
 *
 * @since 1.0.0
 *
 * @type {number}
 */
export const SAVED_MESSAGES_PAGE_SIZE: number = 30;
