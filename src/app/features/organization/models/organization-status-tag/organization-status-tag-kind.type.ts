/**
 * Discriminator for every organization entity enum that renders as a status
 * indicator (badge or select option). Currently the workspace status only.
 */
export type OrganizationStatusTagKind = 'status';
