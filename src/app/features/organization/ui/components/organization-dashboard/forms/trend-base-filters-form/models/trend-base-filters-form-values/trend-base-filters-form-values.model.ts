/**
 * Type TrendBaseFiltersFormValues
 *
 * @description
 * Raw values handled by the shared dashboard base-filters form.
 *
 * @since 1.0.0
 */
export type TrendBaseFiltersFormValues = {
  readonly dateRange: Date[] | null;
  readonly compareEnabled: boolean;
};
