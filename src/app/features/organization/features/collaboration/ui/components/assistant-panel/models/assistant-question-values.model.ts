/**
 * Interface AssistantQuestionValues
 * @interface AssistantQuestionValues
 *
 * @description
 * The panel composer's single field: the question being written.
 *
 * @since 1.0.0
 *
 * @author Valentin FORTIN <contact@valentin-fortin.pro>
 */
export interface AssistantQuestionValues {
  /** What the member has typed so far, unsent. */
  question: string;
}
