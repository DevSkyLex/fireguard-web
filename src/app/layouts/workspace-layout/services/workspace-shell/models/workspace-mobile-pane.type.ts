/**
 * Type WorkspaceMobilePane
 * @type WorkspaceMobilePane
 *
 * @description
 * Which pane the mobile workspace shows. Below the desktop breakpoint the four
 * columns collapse into one viewport: `list` shows the channel sidebar, `main`
 * shows the conversation or page column.
 *
 * @since 1.0.0
 */
export type WorkspaceMobilePane = 'list' | 'main';
