import type { LoginInput } from "@core/models/auth";

/**
 * Type LoginFormValues
 * @type {LoginFormValues}
 * 
 * @description
 * Login form values type (emitted on submit).
 * 
 * @version 1.0.0
 * 
 * @author Valentin FORTIN <contact@valentin-fortin.pro>
 */
export type LoginFormValues = LoginInput;
