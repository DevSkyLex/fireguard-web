/**
 * Discriminator for every organization people-management enum that renders as
 * a status indicator (badge or select option).
 */
export type InvitationTagKind = 'invitationStatus' | 'memberStatus';
