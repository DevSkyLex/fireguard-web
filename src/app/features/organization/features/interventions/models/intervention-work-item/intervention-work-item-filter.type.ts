/**
 * How the field-work table narrows its rows: everything, what is left to do
 * (planned + in progress), what is done, or what was skipped.
 */
export type InterventionWorkItemFilter = 'all' | 'remaining' | 'done' | 'skipped';
