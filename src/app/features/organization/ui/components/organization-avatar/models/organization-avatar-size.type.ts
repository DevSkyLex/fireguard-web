/**
 * The four rungs an organization avatar renders at: 24, 32, 36 and 56px.
 *
 * They are the sizes the feature actually uses — the dense menu row, the
 * default row, the dashboard identity block and the invitation hero — rather
 * than a generic scale nobody would reach for.
 */
export type OrganizationAvatarSize = 'xs' | 'sm' | 'md' | 'lg';
