import { lucideArchive, lucideCircleCheck } from '@ng-icons/lucide';

/**
 * Constant CHECKLIST_STATUS_TAG_ICONS
 *
 * @description
 * Every glyph the checklist status tag registry can resolve to, in one
 * record to hand to `provideIcons()`.
 *
 * @since 1.0.0
 *
 * @type {Readonly<Record<string, string>>}
 */
export const CHECKLIST_STATUS_TAG_ICONS: Readonly<Record<string, string>> = {
  lucideArchive,
  lucideCircleCheck,
};
