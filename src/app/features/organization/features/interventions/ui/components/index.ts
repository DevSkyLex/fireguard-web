/**
 * Intervention UI component public exports.
 *
 * Only the tag is published: it is how any consumer renders an intervention
 * enum through this feature's own registry rather than a copied map.
 */
export { InterventionTag } from './intervention-tag';
